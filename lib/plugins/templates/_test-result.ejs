    <tr>
      <td><%= plugin %></td>
      <td><%= test %></td>
      <% var r = testResults[plugin] &&
                     testResults[plugin][test] &&
                     testResults[plugin][test].result; %>
      <td>
        <span class="label <%= r ? (r === 'pass' ? 'success' : 'warning') : '' %>"><%= r || 'no assertions' %></span>
      </td>
      <td>
        <% if (r) { %>
          <% testResults[plugin][test].passes.forEach(function(a) { %>
            <div><%= a %></div>
          <% }); %>
          <% testResults[plugin][test].failures.forEach(function(a) { %>
            <div><span class="label warning"><%= a %></span></div>
          <% }); %>
        <% } %>
      </td>
    </tr>
